/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Agrega aquí los dominios de tus imágenes cuando conectes el backend
    remotePatterns: [],
  },
};

module.exports = nextConfig;
